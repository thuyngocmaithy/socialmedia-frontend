const images = {
    logo: require('../images/logo.svg').default, //file svg cần .default
    noImage: require('../images/no-image.png'),
    backgroundGray: require('../images/background-gray.png'),
};

export default images;
